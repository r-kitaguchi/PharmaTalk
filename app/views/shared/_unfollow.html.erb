<%= form_with model: current_student.relationships.find_by(pharmacist_id: pharmacist.id),
       html: { method: :delete} do |f| %>
  <div><%= hidden_field_tag :pharmacist_id, pharmacist_profile.pharmacist_id %></div>
  <%= f.submit "トーク申請を解除する", class: "btn unfollow" %>
<% end %>
