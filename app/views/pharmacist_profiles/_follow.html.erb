<%= form_with model: current_student.relationships.build do |f| %>
  <div><%= hidden_field_tag :pharmacist_id, pharmacist_profile.pharmacist_id %></div>
  <%= f.submit "トークを申請する", class: "btn" %>
<% end %>
